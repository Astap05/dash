# 🚀 Тестирование платежей для всех криптовалют

Я реализовал полную поддержку генерации адресов для всех 30+ криптовалют и добавил возможность тестирования платежей.

## Что сделано:

### 1. Генерация адресов
Теперь сервер генерирует корректные форматы адресов для всех поддерживаемых сетей:
- **Bitcoin (BTC)**: адреса начинаются с `1...`
- **Tron (TRX)**: адреса начинаются с `T...`
- **Ripple (XRP)**: адреса начинаются с `r...`
- **Dogecoin (DOGE)**: адреса начинаются с `D...`
- **Monero (XMR)**: адреса начинаются с `4...`
- **Solana (SOL)**: адреса в формате Solana (Base58)
- **EVM (ETH, BNB, MATIC, ARB, AVAX)**: адреса `0x...`
- **Stellar (XLM)**: адреса начинаются с `G...`
- **Zcash (ZEC)**: адреса начинаются с `t1...`
- **Sui (SUI)**: адреса `0x...` (длинные)

### 2. Тестовый режим
Добавлена специальная кнопка для тестирования, которая доступна только в режиме разработки:
- Кнопка: **"[TEST MODE] Simulate Payment Received"**
- Расположение: На странице оплаты, перед кнопками Web3 кошельков
- Функция: Мгновенно помечает инвойс как оплаченный

## Как протестировать:

1. **Выберите любую криптовалюту** в дашборде (например, Monero, Dogecoin или Tron).
2. Нажмите **"Continue"** и затем **"Create Invoice"**.
3. Вы увидите страницу оплаты. Обратите внимание, что **адрес кошелька соответствует формату выбранной сети**.
4. Чтобы проверить успешную оплату, нажмите серую кнопку **"[TEST MODE] Simulate Payment Received"**.
5. Система отправит запрос на сервер, и через 1-2 секунды статус инвойса обновится на "Paid".
6. Появится зеленое модальное окно **"🎉 Оплата успешна!"**.

Теперь вы можете полноценно тестировать пользовательский опыт (UX) для любой из добавленных валют, не тратя реальные средства! 🎉
